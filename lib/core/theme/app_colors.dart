import 'package:flutter/material.dart';

class AppColors {
  // Couleurs principales de la maquette
  static const Color primary = Color(0xFF6B46C1); // Violet principal
  static const Color primaryDark = Color(0xFF553C9A); // Violet foncé
  static const Color primaryLight = Color(0xFF8B5CF6); // Violet clair
  
  static const Color secondary = Color(0xFF8B5CF6); // Violet secondaire
  static const Color secondaryDark = Color(0xFF7C3AED);
  static const Color secondaryLight = Color(0xFFA78BFA);
  
  static const Color background = Color(0xFFF8FAFC); // Fond très clair
  static const Color surface = Color(0xFFFFFFFF);
  static const Color onSurface = Color(0xFF1E293B);
  
  static const Color error = Color(0xFFEF4444);
  static const Color warning = Color(0xFFF59E0B);
  static const Color success = Color(0xFF10B981);
  static const Color info = Color(0xFF3B82F6);
  
  static const Color textPrimary = Color(0xFF1E293B);
  static const Color textSecondary = Color(0xFF64748B);
  static const Color textHint = Color(0xFF94A3B8);
  
  static const Color divider = Color(0xFFE2E8F0);
  static const Color cardBackground = Color(0xFFFFFFFF);
  
  // Couleurs supplémentaires pour la maquette
  static const Color accent = Color(0xFF6B46C1); // Même que primary
  static const Color accentLight = Color(0xFFF3F4F6); // Gris très clair pour les fonds
}
